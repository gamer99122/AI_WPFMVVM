<Window x:Class="RelayCommandExample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RelayCommandExample.ViewModels"
        Title="RelayCommand 範例"
        Height="450"
        Width="550"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 輸入區域 -->
        <GroupBox Grid.Row="0"
                  Header="Command 範例 1: CanExecute 控制"
                  Padding="15">
            <StackPanel>
                <TextBlock Text="輸入文字後，按鈕才會啟用"
                           Foreground="Gray"
                           Margin="0,0,0,10"/>
                <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="5"
                         Margin="0,0,0,10"/>
                <Button Content="顯示訊息"
                        Command="{Binding ShowMessageCommand}"
                        Padding="10,5"
                        FontSize="14"/>
                <TextBlock Text="{Binding DisplayText}"
                           Margin="0,10,0,0"
                           FontSize="14"
                           Foreground="#2C3E50"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </GroupBox>

        <!-- 計數器區域 -->
        <GroupBox Grid.Row="1"
                  Header="Command 範例 2: 多個命令互動"
                  Padding="15"
                  Margin="0,15,0,0">
            <StackPanel>
                <TextBlock FontSize="18"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15">
                    <Run Text="點擊次數: "/>
                    <Run Text="{Binding ClickCount, Mode=OneWay}"
                         FontWeight="Bold"
                         Foreground="#3498DB"
                         FontSize="24"/>
                </TextBlock>

                <UniformGrid Rows="1"
                             Columns="3">
                    <Button Content="增加"
                            Command="{Binding IncrementCommand}"
                            Margin="0,0,5,0"
                            Padding="10,8"
                            FontSize="14"
                            Background="#27AE60"
                            Foreground="White"
                            BorderThickness="0"/>
                    <Button Content="重置"
                            Command="{Binding ResetCommand}"
                            Margin="5,0"
                            Padding="10,8"
                            FontSize="14"
                            Background="#E74C3C"
                            Foreground="White"
                            BorderThickness="0"/>
                    <Button Content="全部清除"
                            Command="{Binding ClearCommand}"
                            Margin="5,0,0,0"
                            Padding="10,8"
                            FontSize="14"
                            Background="#95A5A6"
                            Foreground="White"
                            BorderThickness="0"/>
                </UniformGrid>
            </StackPanel>
        </GroupBox>

        <!-- 說明 -->
        <Border Grid.Row="4"
                Background="#ECF0F1"
                Padding="10"
                CornerRadius="3">
            <TextBlock TextWrapping="Wrap"
                       FontSize="12"
                       Foreground="#7F8C8D">
                <Run Text="💡 提示: "/>
                <Run Text="觀察按鈕的啟用/停用狀態。'顯示訊息'按鈕只有在輸入文字後才啟用，'重置'按鈕只有在計數器大於0時才啟用。"/>
            </TextBlock>
        </Border>
    </Grid>
</Window>
